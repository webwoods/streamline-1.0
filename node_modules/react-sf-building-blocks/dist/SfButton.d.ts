import * as React from 'react';
interface Props {
    variant: string;
    type: string;
    caption: string;
    onClick: (event: any) => void;
    disabled?: boolean;
    theme?: any;
    icon?: React.ReactNode;
    className?: string;
    styles?: any;
}
declare const SfButton: ({ variant, type, disabled, caption, onClick, theme, icon, className, styles }: Props) => JSX.Element;
export default SfButton;
